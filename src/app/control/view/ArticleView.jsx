// import { ArticleViewRef } from '../../view/ArticleView/ArticleView_cmpt';
// import model from '../../model/model';
export default class Control {
    // static async demo() {
    //     const result = await model();
    //     if (typeof result !== 'number') {
    //           let data = eval(`(${result})`);
    //           if (data.code == 0) {
    //                ArticleViewRef.setfunc(data.data);
    //            } else {
    //                ArticleViewRef.interfaceError(data.msg);
    //            }
    //        } else {
    //            ArticleViewRef.networkError(result);
    //        }
    // }
}