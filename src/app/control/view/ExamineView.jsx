// import { ExamineViewRef } from '../../view/ExamineView/ExamineView_cmpt';
// import model from '../../model/model';
export default class Control {
    // static async demo() {
    //     const result = await model();
    //     if (typeof result !== 'number') {
    //           let data = eval(`(${result})`);
    //           if (data.code == 0) {
    //                ExamineViewRef.setfunc(data.data);
    //            } else {
    //                ExamineViewRef.interfaceError(data.msg);
    //            }
    //        } else {
    //            ExamineViewRef.networkError(result);
    //        }
    // }
}