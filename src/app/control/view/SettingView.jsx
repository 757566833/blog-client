// import { SettingViewRef } from '../../view/SettingView/SettingView_cmpt';
// import model from '../../model/model';
export default class Control {
    // static async demo() {
    //     const result = await model();
    //     if (typeof result !== 'number') {
    //           let data = eval(`(${result})`);
    //           if (data.code == 0) {
    //                SettingViewRef.setfunc(data.data);
    //            } else {
    //                SettingViewRef.interfaceError(data.msg);
    //            }
    //        } else {
    //            SettingViewRef.networkError(result);
    //        }
    // }
}