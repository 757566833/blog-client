// import { WriteViewRef } from '../../view/WriteView/WriteView_cmpt';
// import model from '../../model/model';
export default class Control {
    // static async demo() {
    //     const result = await model();
    //     if (typeof result !== 'number') {
    //           let data = eval(`(${result})`);
    //           if (data.code == 0) {
    //                WriteViewRef.setfunc(data.data);
    //            } else {
    //                WriteViewRef.interfaceError(data.msg);
    //            }
    //        } else {
    //            WriteViewRef.networkError(result);
    //        }
    // }
}