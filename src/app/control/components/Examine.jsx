// import { ExamineRef } from '../../components/Examine/Examine_cmpt';
// import model from '../../model/model';
export default class Control {
    // static async demo() {
    //     const result = await model();
    //     if (typeof result !== 'number') {
    //           let data = eval(`(${result})`);
    //           if (data.code == 0) {
    //                ExamineRef.setfunc(data.data);
    //            } else {
    //                ExamineRef.interfaceError(data.msg);
    //            }
    //        } else {
    //            ExamineRef.networkError(result);
    //        }
    // }
}