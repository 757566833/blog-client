// import { SvgRef } from '../../components/Svg/Svg_cmpt';
// import model from '../../model/model';
export default class Control {
    // static async demo() {
    //     const result = await model();
    //     if (typeof result !== 'number') {
    //            let data = eval('(' + result + ')');
    //           if (data.code == 0) {
    //                SvgRef.setfunc(data.data);
    //            } else {
    //                SvgRef.interfaceError(data.msg);
    //            }
    //        } else {
    //            SvgRef.networkError(result);
    //        }
    // }
}